[package]
name = "pylon-cache"
version = "0.1.0"
edition = "2021"

[dependencies]
heed = "0.22"
rkyv = { version = "0.8", default-features = false, features = ["std"] }
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
tempfile = "3"
